<Page x:Class="menza_admin.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:menza_admin"
      mc:Ignorable="d"
      Title="Orders">
    <Grid Margin="20" Background="#F5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Rendelések" FontSize="32" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,10"/>
            <TextBlock x:Name="DateDisplay" FontSize="14" Foreground="#666666"/>
        </StackPanel>

        <!-- Filter and Refresh Section -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15" FlowDirection="LeftToRight">
            <TextBlock Text="Dátum kiválasztása:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#333333"/>
            <DatePicker x:Name="OrderDatePicker" Width="150" Height="35" Margin="0,0,10,0" 
                        SelectedDateChanged="OrderDatePicker_SelectedDateChanged"/>
            <Button x:Name="RefreshButton" Content="🔄 Frissítés" Width="110" Height="35" 
                    Margin="0,0,10,0" Click="RefreshButton_Click" Cursor="Hand"
                    Background="#3A8DFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#3A8DFF"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5AA3FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7DD6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="ExportButton" Content="📊 PDF Exportálás" Width="140" Height="35" 
                    Margin="0,0,10,0" Click="ExportButton_Click" Cursor="Hand"
                    Background="#3A8DFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#3A8DFF"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5AA3FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7DD6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBlock Text="Összes rendelés:" VerticalAlignment="Center" Margin="20,0,5,0" FontWeight="Bold" Foreground="#333333"/>
            <TextBlock x:Name="TotalOrdersText" Text="0" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Foreground="#3A8DFF"/>
        </StackPanel>

        <!-- Orders List -->
        <Border Grid.Row="2" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="8">
            <Grid>
                <!-- Loading Indicator -->
                <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="⌛" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Rendelések betöltése..." FontSize="16" Foreground="#666666"/>
                </StackPanel>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="📭" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Nincs rendelés erre a napra" FontSize="16" Foreground="#666666"/>
                </StackPanel>

                <!-- Error State -->
                <StackPanel x:Name="ErrorPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="❌" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock x:Name="ErrorText" Text="Hiba a rendelések betöltése során" FontSize="16" Foreground="#E53935" TextWrapping="Wrap" MaxWidth="400" TextAlignment="Center"/>
                    <Button Content="Újra próbálom" Width="120" Height="35" Margin="0,15,0,0" Click="RefreshButton_Click" 
                            Cursor="Hand" Background="#3A8DFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#3A8DFF"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5AA3FF"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#2E7DD6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Orders DataGrid -->
                <DataGrid x:Name="OrdersDataGrid" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          VerticalGridLinesBrush="#E0E0E0"
                          HorizontalGridLinesBrush="#E0E0E0"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FAFAFA"
                          Background="White"
                          RowBackground="White"
                          RowHeight="45"
                          FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Étel Neve" Binding="{Binding Name}" Width="*" MinWidth="150">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="10,10"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Ár" Binding="{Binding PriceFormatted}" Width="110">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="10,10"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Mennyiség" Binding="{Binding TodayQuantity}" Width="110">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="10,10"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#3A8DFF"/>
                                    <Setter Property="FontSize" Value="15"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Összesen" Binding="{Binding TodayRevenueFormatted}" Width="130">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="10,10"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,0"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Summary Section -->
        <Border Grid.Row="3" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15" Margin="0,15,0,0" CornerRadius="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Összes bevétel:" FontWeight="Bold" FontSize="18" Foreground="#333333" Margin="0,0,10,0"/>
                <TextBlock x:Name="TotalRevenueText" Text="0 Ft" FontSize="18" FontWeight="Bold" Foreground="#2E7D32"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
