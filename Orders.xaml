<Page x:Class="menza_admin.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:menza_admin"
      mc:Ignorable="d"
      Title="Orders" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Orders" FontSize="32" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="DateDisplay" FontSize="16" Foreground="Gray"/>
        </StackPanel>

        <!-- Filter and Refresh Section -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Select Date:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <DatePicker x:Name="OrderDatePicker" Width="150" Height="35" Margin="0,0,10,0" 
                        SelectedDateChanged="OrderDatePicker_SelectedDateChanged"/>
            <Button x:Name="RefreshButton" Content="🔄 Refresh" Width="100" Height="35" 
                    Margin="0,0,10,0" Click="RefreshButton_Click"/>
            <Button x:Name="ExportButton" Content="📊 Export" Width="100" Height="35" 
                    Margin="0,0,10,0" Click="ExportButton_Click"/>
            <TextBlock Text="Total Orders:" VerticalAlignment="Center" Margin="20,0,5,0" FontWeight="Bold"/>
            <TextBlock x:Name="TotalOrdersText" Text="0" VerticalAlignment="Center" FontSize="16" Foreground="#3A8DFF"/>
        </StackPanel>

        <!-- Orders List -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5">
            <Grid>
                <!-- Loading Indicator -->
                <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="⌛" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Loading orders..." FontSize="16" Foreground="Gray"/>
                </StackPanel>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="📭" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="No orders for this date" FontSize="16" Foreground="Gray"/>
                </StackPanel>

                <!-- Error State -->
                <StackPanel x:Name="ErrorPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="❌" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock x:Name="ErrorText" Text="Failed to load orders" FontSize="16" Foreground="Red" TextWrapping="Wrap" MaxWidth="400" TextAlignment="Center"/>
                    <Button Content="Try Again" Width="100" Height="30" Margin="0,10,0,0" Click="RefreshButton_Click"/>
                </StackPanel>

                <!-- Orders DataGrid -->
                <DataGrid x:Name="OrdersDataGrid" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5"
                          RowHeight="40"
                          FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Food Name" Binding="{Binding Name}" Width="*" MinWidth="150">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Price" Binding="{Binding PriceFormatted}" Width="100">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Quantity" Binding="{Binding TodayQuantity}" Width="100">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#3A8DFF"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Total Revenue" Binding="{Binding TodayRevenueFormatted}" Width="120">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="Background" Value="#3A8DFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Summary Section -->
        <Border Grid.Row="3" Background="#F0F0F0" Padding="15" Margin="0,15,0,0" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total Revenue:" FontWeight="Bold" FontSize="18" Margin="0,0,10,0"/>
                <TextBlock x:Name="TotalRevenueText" Text="0 Ft" FontSize="18" FontWeight="Bold" Foreground="#27AE60"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
